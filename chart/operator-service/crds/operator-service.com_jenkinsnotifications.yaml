
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: jenkinsnotifications.operator-service.com
spec:
  group: operator-service.com
  names:
    kind: JenkinsNotification
    listKind: JenkinsNotificationList
    plural: jenkinsnotifications
    singular: jenkinsnotification
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: 'JenkinsNotification is a service configuration used to send
          notifications about Jenkins configuration status. Docs: https://jenkinsci.github.io/kubernetes-operator/docs/getting-started/latest/notifications/'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'JenkinsNotificationSpec is a service configuration used
              to send notifications about Jenkins configuration status. Docs: https://jenkinsci.github.io/kubernetes-operator/docs/getting-started/latest/notifications/'
            properties:
              level:
                description: NotificationLevel defines the level of a Notification.
                type: string
              mailgun:
                description: Mailgun is handler for Mailgun email service notification
                  channel.
                properties:
                  apiKeySecretKeySelector:
                    description: SecretKeySelector selects a key of a Secret.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      secret:
                        description: The name of the secret in the pod's namespace
                          to select from.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                    required:
                    - key
                    - secret
                    type: object
                  domain:
                    type: string
                  from:
                    type: string
                  recipient:
                    type: string
                required:
                - apiKeySecretKeySelector
                - domain
                - from
                - recipient
                type: object
              name:
                type: string
              slack:
                description: Slack is handler for Slack notification channel.
                properties:
                  webHookURLSecretKeySelector:
                    description: The web hook URL to Slack App
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      secret:
                        description: The name of the secret in the pod's namespace
                          to select from.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                    required:
                    - key
                    - secret
                    type: object
                required:
                - webHookURLSecretKeySelector
                type: object
              smtp:
                description: SMTP is handler for sending emails via this protocol.
                properties:
                  from:
                    type: string
                  passwordSecretKeySelector:
                    description: SecretKeySelector selects a key of a Secret.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      secret:
                        description: The name of the secret in the pod's namespace
                          to select from.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                    required:
                    - key
                    - secret
                    type: object
                  port:
                    type: integer
                  server:
                    type: string
                  tlsInsecureSkipVerify:
                    type: boolean
                  to:
                    type: string
                  usernameSecretKeySelector:
                    description: SecretKeySelector selects a key of a Secret.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      secret:
                        description: The name of the secret in the pod's namespace
                          to select from.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                    required:
                    - key
                    - secret
                    type: object
                required:
                - from
                - passwordSecretKeySelector
                - port
                - server
                - to
                - usernameSecretKeySelector
                type: object
              teams:
                description: MicrosoftTeams is handler for Microsoft MicrosoftTeams
                  notification channel.
                properties:
                  webHookURLSecretKeySelector:
                    description: The web hook URL to MicrosoftTeams App
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      secret:
                        description: The name of the secret in the pod's namespace
                          to select from.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                    required:
                    - key
                    - secret
                    type: object
                required:
                - webHookURLSecretKeySelector
                type: object
              verbose:
                type: boolean
            required:
            - level
            - name
            - verbose
            type: object
          status:
            description: JenkinsNotificationStatus defines the observed state of JenkinsNotification
            properties:
              conditions:
                description: Conditions contains JenkinsNotification statuses
                items:
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: 'Status is the status of the condition. Can be
                        True, False, Unknown. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions'
                      type: string
                    type:
                      description: 'Type is the type of the condition. More info:
                        https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions'
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              operatorVersion:
                description: OperatorVersion is the operator version which manages
                  this CR
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
